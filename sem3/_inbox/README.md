# Beeper WhatsApp inbox

Pending downloads land in `pending/`. Approval queue JSON is in `queue/`.
After you approve via `tools/beeper-ingest/ingest.py review`, files move into the matching course `resources/` folder (or `unsorted/` here).
